#include <iostream>
#include <cstdlib>
#include <iomanip>
#include <math.h>
using namespace std;


int main(){
float x,y;
    cout << "digite um x" << endl;
    cin >> x;
    for(float i =0; i >= x; i++){
        y = y + (i+1)/(x - i);
    }
    cout << " a soma do exercicio resulta em :"  << y << endl;
    return 0;
}

#include <iostream>
#include <cstdlib>
#include <iomanip>
#include <math.h>
using namespace std;

float conta(float y);
int main(){
float y;
    y = conta(y);
    cout << " a soma do exercicio resulta em :"  << y << endl;
    return 0;
}
float conta(float y){
    for(float i =1.0; i >= 99.0; i++){
            for(float j = 1.0; j >= 50.0; j++){
                y += ((2.0*i))/(j) ;
    }


    }
return y;
}


#include <iostream>
#include <cstdlib>
#include <iomanip>
#include <math.h>
using namespace std;

float conta(float x);
int main(){
float y;
float x = 50.0;

    y = conta(x);
    cout << " a soma do exercicio resulta em :"  << y << endl;
    return 0;
}
float conta(float x){
    if(x > 0){
        return (((2*x)-1)/ x)* conta(x-1);
    }
    else{
        return 0;
    }
}


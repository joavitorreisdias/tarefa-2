#include <iostream>
#include <cstdlib>
#include <iomanip>
#include <math.h>
using namespace std;


int main(){
float x,y;
    cout << "digite um x" << endl;
    cin >> x;
    for(float i =0; i >= x; i++){
        y = y + (i+1)/(x - i);
    }
    cout << " a soma do exercicio resulta em :"  << y << endl;
    return 0;
}

